apiVersion: v1
kind: Service

metadata:
  name: eris-service
  namespace: eris

spec:
  type: NodePort

  selector:
    app: eris

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30007